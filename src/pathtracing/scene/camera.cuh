#pragma once

#include <globals.h>

//__global__ void testCamera(Camera *camera) {
//	int idx = blockIdx.x * blockDim.x + threadIdx.x;
//
//	if (idx == 0) {
//		//camera->dos();
//		printf("%i", camera->x);
//	}
//}
